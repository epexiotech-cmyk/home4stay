const fs = require('fs'); const file = '\\\\wsl$\\Ubuntu\\home\\apurv_patel\\home4stay\\apps\\main-site\\src\\app\\api\\auth\\login\\route.ts'; let c = fs.readFileSync(file, 'utf8'); c = c.replace(/,\s*loginType:\s*z\.enum\(\[\customer\,\s*\partner\,\s*\dmin\\]\)/, ''); c = c.replace('const { email, password, loginType } = validation.data;', 'const { email, password } = validation.data;'); c = c.replace('authResult = await authService.login(email, password, loginType, ip, userAgent, requestId);', 'authResult = await authService.login(email, password, ip, userAgent, requestId);'); fs.writeFileSync(file, c);
